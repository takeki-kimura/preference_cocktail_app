<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Cocktail</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet">
  </head>

  <body>
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <%= link_to image_tag("logo.png", class: :logo), root_path %>
          </div>
          <% if user_signed_in? %>
            <div class="nav__right">
              <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: :nav__logout %>
              <%= link_to "New Cocktail", new_cocktail_path, class: :nav__btn %>
            </div>
          <% else %>
            <div class="nav__right">
              <%= link_to new_user_session_path, method: :get, class: :nav__btn do %>
                <i class="fa fa-user "></i>  ログイン
              <% end %>
              <%= link_to new_user_registration_path, method: :get, class: :nav__btn do %>
                <i class="fa fa-user-plus"></i> 新規登録
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    </header>
    <%= yield %>
    <footer class="footer">
      <p class="copyright">お酒は20歳になってから！</p>
    </footer>
  </body>
</html>
